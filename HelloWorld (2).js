var b, h, a;
    
    // b = base
    // h = height
    // a = area
    
    b = 42;
    h = 12;
    
    // we Calculate area of the triangle
    a = ((b * h) / 2);
    
    console.log("Area of the triangle = " + a + " sq. units");